# CLAUDE.md — 作業ルール

このファイルは、本リポジトリで作業するAIエージェント（Claude Code）の**作業ルールのSource of Truth**である。セッション開始時に自動で読み込まれる。ここに書かれたルールは、他のドキュメントの記述より優先する。

---

## 1. プロジェクト概要

- **対象**: AIの鉄人株式会社（株式会社AIの鉄人）コーポレートサイト
- **公開URL**: https://www.ainotetsujin.com/
- **配信構成**: GitHub Pages（リポジトリ `kzkzkn/HP`、`main` ブランチを配信）。独自ドメインは `CNAME` で設定。
- **現在の実装**: 素のHTML/CSS/Vanilla JSによる1ページLP（`index.html` + `assets/`）。ビルド工程・パッケージマネージャは無い。
- **フレームワーク**: Astro等への移行は現時点で行わない。ページ数が増え、ヘッダー・フッター・メタタグ等の共通パーツの手動同期が負担になった時点で検討する。
- **状況**: **全面リニューアル計画が進行中**。計画書はリポジトリ外（`コーポレートサイト_リニューアル_プランニング書_v2.md`）にあり、リニューアル後の目標体系はこのファイルの §2 に要約してある。
- **外部依存**: Google Fonts（Noto Sans JP / IBM Plex Mono）のみ。外部JSライブラリ・フレームワークは追加しない。
- **問い合わせ導線**: Googleフォーム（`docs/content.md` のURLを使用）。サイト内に独自フォームは持たない。

---

## 2. ドキュメント体系

### 2.1 目標体系（リニューアル後）

| ファイル | 役割 |
| --- | --- |
| `CLAUDE.md` | エージェント作業ルール（本ファイル） |
| `DESIGN.md` | 見た目のSource of Truth（Color / Typography / Spacing / Grid / Components / Motion / Responsive / Do・Don't） |
| `docs/CONTENT.md` | 事実のSource of Truth（会社概要・サービス・実績・数字・顧客名・CTA） |
| `docs/BRAND.md` | 言語表現・思想のルール（トーン、使う言葉／使わない言葉、Mission / Vision） |
| `docs/SITE.md` | 情報設計（Sitemap、各ページの目的・Target・CTA・Section構成） |
| `docs/ACCEPTANCE-CRITERIA.md` | 受け入れ基準 |
| `.claude/skills/` | 定型タスクのスキル |
| `.claude/settings.json` | 権限・hooks設定 |

### 2.2 現状（重要）

**上記の `DESIGN.md` / `docs/CONTENT.md` / `docs/BRAND.md` / `docs/SITE.md` / `docs/ACCEPTANCE-CRITERIA.md` はまだ存在しない。** 現時点では、既存の以下のファイルを**暫定のSource of Truth**として参照する。

| 現在のファイル | 役割 | 将来の移行先 |
| --- | --- | --- |
| `docs/design-spec.md` | 現行サイトのデザイン仕様。CSS変数（トークン）、タイポグラフィのサイズ表、セクション別レイアウト数値、レスポンシブ挙動（≤1024 / ≤768 / ≤480px）、インタラクション、禁止事項。**見た目の暫定SoT** | `DESIGN.md` |
| `docs/content.md` | サービス資料（両面チラシPDF）から転記した確定コピー。メタ情報、各セクションの文言、会社概要、公開前TODO。**「一字一句そのまま使用。要約・言い換え・追記は禁止」と明記されている。事実の暫定SoT** | `docs/CONTENT.md` |
| `docs/asset-ledger.md` | 素材台帳。配置済み画像の出典と加工履歴、SVGで実装した要素、生成画像の仕様・プロンプト・採否レビュー基準。**画像を追加・差し替えるときは必ずここを更新する** | 継続利用（`DESIGN.md` の Photography と併用） |
| `docs/review-log.md` | ビジュアルレビューと機能QAの記録。セクション別の一致度・指摘・PASS判定、既知の誤検知メモ。**過去の判断理由を遡るための記録** | 継続利用 |
| `docs/tasks/CT-*.md` | 現行サイト構築時のタスク指示書（履歴）。**現行実装の意図を確認する参考資料であり、これから守るべきルールではない** | アーカイブ |

移行方針: **Phase 1 で `DESIGN.md`、Phase 2 で `docs/SITE.md` と `docs/CONTENT.md` を作成**し、新体系へ移す。移行が完了するまでは上表の暫定SoTを正とする。新体系のファイルを作成したら、このセクションと §3 の参照先を同時に更新する。

同じ内容を複数ファイルに書かない。事実は事実のSoTに、見た目は見た目のSoTに、それぞれ1箇所だけ書く。

> `AGENTS.md` は「作業ルールは `CLAUDE.md` を参照」とだけ書いた薄いポインタファイルである。実質的な記述は必ず本ファイル側にのみ書き、内容を重複させない。

---

## 3. 作業ルール（必須）

1. **UI・スタイルを変更する前に `DESIGN.md` を読む。** 未作成の間は `docs/design-spec.md` を読む。トークン（色・フォント・余白・グリッド）は自分で決めず、必ずそこの定義に従う。定義が無い値が必要になったら、勝手に足さず人間に確認する。
2. **会社の事実を創作しない。** 実績・数字・顧客名・受賞歴・サービス内容・設立年・資本金・電話番号・導入社数などは、`docs/CONTENT.md`（未作成の間は `docs/content.md`）に書かれているもの**だけ**を使う。他の資料や推測から補わない。**不明な事実は「要確認」として人間に確認する**（勝手に埋めない、プレースホルダーで本番に出さない）。
3. **`main` へ直接pushしない。** 必ず feature branch を切り、Pull Request 経由で変更する（`main` はブランチ保護済みで技術的にも拒否される）。
4. **影響範囲の大きい変更は Plan Mode を使う。** トップページの全面実装、デザイン方向の切り替え、情報設計の変更などは、まず計画を提示して**人間のレビューを受けてから**実装に入る。「作ってから直す」ではなく「合意してから作る」。軽微な文言修正やスタイル微調整には使わない。
5. **PR作成前に `/code-review` を実行する。** フォーム・外部連携・スクリプトに触れる変更では `/security-review` も実行する。指摘への対応を完了させてから人間レビューへ回す。
6. **調査系の重い作業はサブエージェントに委任する。** 既存HTML/CSSの棚卸し、参考サイト調査、リンク切れの全走査、アクセシビリティ一括チェックなどは「調べて要点だけ返す」役割で委任し、実装本体は本体セッションで行う。
7. **素材を追加・差し替えたら `docs/asset-ledger.md` を更新する。** 出典・加工内容・用途を記録する。
8. **AI生成画像と汎用ストックフォトを使わない。** 写真が不足している面は、タイポグラフィ・余白・罫線・単色面・図版で構成する。写真が入る前提の枠にはマークアップ上にコメント（例: `<!-- PHOTO: 店舗訪問 -->`）を残し、後から差し替えやすくする。
9. **実在他社のロゴを掲載しない。** 顧客企業は文字情報のみで扱い、掲載可否は許諾の範囲内に限る。

---

## 4. 品質チェック（変更後に必ず実施）

### 4.1 レスポンシブ

内蔵ブラウザでページを開き、以下の幅でスクリーンショットを取得して**目視確認**する。

- **375px / 390px**（mobile）
- **768px**（tablet）
- **1280px / 1440px**（desktop）

確認観点: レイアウト崩れ、テキストの折り返し、タップターゲットの大きさ（44px以上）、画像の縦横比。

### 4.2 チェックリスト

- Horizontal overflow なし（全幅で `overflow-x` が発生しない）
- Broken link なし（アンカー切れ・4xx応答を含む）
- Console error なし
- 画像に `alt`、`width` / `height`、`loading="lazy"` が設定されている
- Semantic HTML（H1は1つ、見出し階層が正しい）
- `title` / `meta description` / `canonical` / OGP / favicon / 構造化データ を維持または更新する
- `sitemap.xml` / `robots.txt` の整合
- `prefers-reduced-motion: reduce` でアニメーションが無効になる

### 4.3 Lighthouse

npm の `lighthouse` CLI で計測し、結果をPRに記載する。

| 項目 | 目標 |
| --- | --- |
| Performance | 90以上 |
| Accessibility | 95以上 |
| Best Practices | 95以上 |
| SEO | 95以上 |

---

## 5. デザイン禁止事項

AIが「それっぽいSaaSサイト」を生成することを防ぐため、以下を原則禁止する。

**Do Not（ビジュアル）**

- 紫・青のAIグラデーション
- 光る球体
- AI脳・ロボット・回路図の汎用イメージ
- 架空のビジネスミーティング画像・汎用ストックフォト・AI生成画像
- 過剰なGlassmorphism / 過剰な角丸 / Pill型UIの乱用
- 全セクションをカード化する
- 意味のないアイコン
- 長文の中央揃え
- 無意味なアニメーション・過剰なスクロール演出

**Do Not（コンテンツ）— 特に厳守**

- **架空の導入実績**
- **架空の数字**
- **架空の顧客コメント**
- **架空の受賞歴**

事実は `docs/content.md`（将来 `docs/CONTENT.md`）にあるものだけ。無いものは書かない。**「それらしく埋める」は最も重大な違反**とみなす。

**その他**

- アクセント赤（`--accent`）を小サイズ本文に使わない（コントラスト不足。必要な場合は `--accent-dark`）
- 外部JSライブラリ・フレームワークを追加しない（Vanilla JSのみ。外部読み込みはGoogle Fontsのみ）

---

## 6. コミット・PR規約

### ブランチ

`main` から切り、用途別プレフィックスを付ける。

| プレフィックス | 用途 |
| --- | --- |
| `feat/` | 機能・セクションの追加 |
| `fix/` | 不具合修正 |
| `chore/` | 設定・ドキュメント整備などの雑務 |
| `docs/` | ドキュメントのみの変更 |
| `design/` | デザイン探索ブランチ（例: `design/direction-a-editorial`） |

例: `feat/case-study-fuji-subaru` / `fix/mobile-nav-overflow` / `chore/add-claude-md`

### コミット

- **日本語のコミットメッセージで可**。何を変えたかを一行目で言い切る。
- 1コミット1目的。無関係な変更を混ぜない。
- 生成物・秘匿情報をコミットしない（`.gitignore` を確認する）。

### Pull Request

- タイトルと本文は日本語。
- 本文に**変更概要**と**確認観点**（レビュアーが何を見ればよいか）を書く。
- **UI変更を含む場合はスクリーンショットを添付**する（最低 375px と 1280px）。Lighthouseを実行した場合はスコアも記載する。
- `/code-review`（必要に応じて `/security-review`）を実行済みであることを本文に明記する。
- **merge は人間が行う。エージェントは `gh pr merge` を実行しない。**

### ワークフロー全体

```text
Issue（gh CLI で作成）
  ↓
feature branch を作成し実装
  ↓
/code-review ・ /security-review
  ↓
品質チェック（§4）
  ↓
Pull Request（gh pr create）
  ↓
Human Review
  ↓
Merge → GitHub Pages → 本番反映
```
