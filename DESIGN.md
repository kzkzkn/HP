# DESIGN.md — 見た目のSource of Truth

株式会社AIの鉄人 コーポレートサイト（`ainotetsujin.com`）の**ビジュアルデザインのSource of Truth**である。
色・タイポグラフィ・余白・グリッド・コンポーネント・写真・モーション・レスポンシブ・アクセシビリティの決定は、すべてこのファイルに集約する。

- **バージョン**: v1（Phase 1 — Brand & Design Definition の成果物）
- **上位ドキュメント**: 作業ルールは `CLAUDE.md`、事実は `docs/CONTENT.md`、情報設計は `docs/SITE.md`。**同じ内容をこのファイルと他のSoTに二重に書かない。**
- **根拠**: `コーポレートサイト_リニューアル_プランニング書_v2.md` §6（デザインコンセプト）/ §7（参考デザイン）/ §8（Visual Direction）/ §9（禁止事項）/ §11（トップページ構成）/ §18（制作プロセス）

---

## 0. このファイルの読み方

### 0.1 二層構造（重要）

本サイトは Phase 3 で**3方向のデザイン探索**を行い、Phase 4 のトップページ実装でデザインを確定する流れになっている。

| Direction | 方向性 | ブランチ |
| --- | --- | --- |
| A | Editorial / Takram寄り | `design/direction-a-editorial` |
| B | Technology / LayerX寄り | `design/direction-b-technology` |
| C | Field / CADDi寄り | `design/direction-c-field` |

したがって v1 の記述は次の2層に分かれる。**必ずマーカーを確認してから使うこと。**

| マーカー | 意味 | 変更の扱い |
| --- | --- | --- |
| **[確定]** | どの方向を選んでも変わらない原則・制約・基準。Phase 3の3案すべてがこれを満たさなければならない | 変更には人間の合意が必要。エージェントが勝手に外さない |
| **[暫定]** | 方向選択に依存する値。**初期値は提案済み**なので、決まっていない値として扱わず「いまはこの値で実装する」 | Phase 3の探索で振れてよい。Phase 4で確定し、このファイルを更新する |

- **[暫定] は「未定」ではない。** 書かれた初期値をそのまま使う。値を変えたい場合は、変えた理由をPRに書く。
- **[確定] に反する実装は、たとえ見栄えが良くてもレビューで差し戻す。**
- ここに定義の無い値が必要になったら、**勝手に足さず人間に確認する**（`CLAUDE.md` §3-1）。追加が決まったら、実装より先にこのファイルへ書く。

### 0.2 移行期の扱い

`docs/design-spec.md` は**現行サイト（`main` の `index.html`）の実装記録**として保持する。

- リニューアル実装（Phase 3以降のブランチ・新規ページ）→ **本ファイルに従う。**
- 現行 `index.html` への軽微な保守（誤字修正・リンク差し替え等）→ リニューアルが本番反映されるまでは **`docs/design-spec.md` を参照する。**
- 現行サイトを本ファイルのトークンに合わせて書き換える作業は、Phase 4 の実装で一括して行う。**部分的な混在をさせない。**

---

## 1. Visual Theme

### 1.1 コンセプト

> **Field × Technology**

先端テクノロジーと、リアルな業務現場が同居するブランド。「AIの未来感」だけでも、「地方企業の泥臭い現場感」だけでもない。

**[確定] 優先順位**

- 「未来感」よりも **精度（Precise）**
- 「派手さ」よりも **自信（Confident）**
- 「AIっぽさ」よりも **実際に仕事を変えている感**

**[確定] デザインキーワード**

Editorial / Precise / Confident / Modern / Field-oriented / Understated Technology / Japanese B2B / Premium / Human / Industrial

### 1.2 参考デザインの配分と、借りる要素

**[確定]** 特定サイトの模倣はしない。以下は「借りる要素」の定義であり、レイアウトのコピー元ではない。

| 参考 | 配分 | 借りる要素 | 実地観察で確認できたこと |
| --- | --- | --- | --- |
| **Takram** | 35% | 大胆な余白／Typography中心の表現／写真の使い方／**カードUIへの非依存**／角丸に頼らない構成／Editorialなレイアウト／**色ではなく余白と構図で階層をつくる** | プロジェクト一覧は枠線・カードを持たず、「タイトル＋小さなカテゴリラベル」の対で整然と列挙される。白背景・黒/濃灰テキスト、アクセントは極小 |
| **LayerX** | 25% | 日本のB2B企業としての信頼感／AI企業としての先進性／**Mission・Visionの見せ方**／企業ブランドとしてのスケール感／短く強いコピー／余白を生かした情報設計 | **角丸をほぼ使わない直線的な構成**。白・淡色背景に濃紺〜黒のテキスト、アクセントは最小限（リンクの視線誘導のみ）。セクション見出しを日本語＋英字で併記。4カラムグリッド |
| **CADDi** | 20% | 現場 × Technology／**実際の産業現場写真**／**数字を使った実績表現**／ケーススタディ／業界課題とテクノロジーの接続 | セクションを `01 - PLATFORM` のように**連番＋英字ラベル**で刻み、垂直方向のリズムをつくる。実績は数値を大きく単独で見せる。事例も `Case - 01` と連番管理 |
| **SmartHR** | 15% | **日本語Typography／本文の可読性／アクセシビリティ／行間・字間**／UIの安定性／説明コンテンツの読みやすさ | 冒頭に本文へのスキップリンク、段階的な見出し構造、日英切替。構造で可読性を担保する設計 |
| **Novasell** | 5% | 大胆な数字／巨大Typography／一画面一メッセージ。**ケーススタディと実績数値の強調にのみ限定使用** | 画面を占有する巨大英字見出しと、成果指標の数値表現 |

**[確定]** Novasell的な巨大表現は**実績数値とケーススタディの見出しに限定**する。トップページ全体をこの調子にしない。

### 1.3 全体の印象を決める3つの手段

**[確定]** 本サイトの階層と印象は、次の3つで作る。**装飾（影・グラデーション・枠線の多用）で作らない。**

1. **余白** — セクション間・要素間の距離差で情報のまとまりを示す
2. **タイポグラフィのサイズ差** — 大見出しと小ラベルの落差で視線の順番を作る
3. **罫線とベースラインの整列** — 1pxの水平罫線と、揃った左端で構造を示す

---

## 2. Color

### 2.1 面積比の原則

**[確定]** ページ全体の色の占有比率を次の範囲に収める。Phase 3 の3案すべてがこれを満たすこと。

| 領域 | 比率 |
| --- | --- |
| White / Off White | **70〜80%** |
| Black / Navy（テキスト・反転面・罫線） | **15〜25%** |
| Brand Accent | **5%以下** |

**[確定] 色に関する制約**

- アクセントカラーは**ロゴ由来の赤系1色のみ**。第2・第3のアクセント色を増やさない。
- **紫・青系のAIグラデーションを主役にしない**（`CLAUDE.md` §5）。グラデーション自体を原則使わない。
- **色だけで情報を伝えない**（リンク・状態・強調は、下線・太さ・位置・ラベルを併用する）。
- アクセント色で**面を大きく塗らない**。5%制約は「小さな面積の強い色」を意味する。ベタ塗りの反転カードや、セクション背景全面のアクセント使用は不可。

### 2.2 トークン初期値

**[暫定]** 具体的なHex値は Phase 3 で振れてよい。ただし §2.1 の比率原則と §2.3 のコントラスト基準は [確定] であり、値を変える場合も必ず満たすこと。

```css
:root {
  /* --- Base surface --- */
  --bg:            #FFFFFF;  /* [暫定] 基本背景。ページの主役 */
  --bg-subtle:     #F7F6F3;  /* [暫定] Off White。交互セクション・引用面 */
  --bg-invert:     #14171A;  /* [暫定] 反転セクション（Mission等）の背景 */

  /* --- Ink --- */
  --ink:           #14171A;  /* [暫定] 見出し・本文。純黒を避けたニュートラルブラック */
  --ink-muted:     #5A6068;  /* [暫定] 補助テキスト・注釈 */
  --ink-invert:    #FFFFFF;  /* 反転面のテキスト */

  /* --- Line --- */
  --line:          #E3E3E0;  /* [暫定] 装飾罫線・区切り線（非UI） */
  --line-ui:       #7F858B;  /* [暫定] 操作可能要素の境界（3:1 以上を満たす） */
  --line-strong:   #14171A;  /* [暫定] 構造を示す強い罫線（セクション境界等） */

  /* --- Accent（ロゴ由来の赤。1色のみ） --- */
  --accent:        #DE2B1C;  /* [暫定] ロゴ赤の実測値（design-spec.md より継承） */
  --accent-dark:   #B72217;  /* [暫定] 小サイズ文字・Off White上で赤が必要なとき */

  /* --- Focus --- */
  --focus:         #14171A;  /* [暫定] フォーカスリング色。背景により反転 */
}
```

**[暫定]** `--ink` を **ニュートラルブラック（#14171A）** とするか **ネイビー（#0E1B2E 系）** とするかは、Phase 3 の方向選択と連動する。B（Technology / LayerX寄り）ではネイビー、A・Cではニュートラルブラックが自然。v1 の初期値はニュートラルブラック。

### 2.3 コントラスト基準

**[確定]** WCAG 2.1 AA を満たす。実測値（v1 の初期トークンでの計算値）を併記する。

| 組み合わせ | 比率 | 判定 |
| --- | --- | --- |
| `--ink` #14171A on `--bg` #FFFFFF | 18.0:1 | 本文可 |
| `--ink-muted` #5A6068 on `--bg` #FFFFFF | 6.4:1 | 本文可 |
| `--ink-muted` #5A6068 on `--bg-subtle` #F7F6F3 | 5.9:1 | 本文可 |
| `--accent` #DE2B1C on `--bg` #FFFFFF | 4.7:1 | ぎりぎり可だが**本文使用は禁止**（§2.4） |
| **`--accent` #DE2B1C on `--bg-subtle` #F7F6F3** | **4.3:1** | **不可。AA未達** |
| `--accent-dark` #B72217 on `--bg` #FFFFFF | 6.5:1 | 本文可 |
| `--accent-dark` #B72217 on `--bg-subtle` #F7F6F3 | 6.0:1 | 本文可 |
| `--ink-invert` #FFFFFF on `--accent` #DE2B1C | 4.7:1 | ボタン・大見出しのみ可 |
| `--line-ui` #7F858B on `--bg-subtle` #F7F6F3 | 3.5:1 | UI境界として可（3:1以上） |
| `--line` #E3E3E0 on `--bg` #FFFFFF | 1.3:1 | **装飾罫線専用。** UI境界に使わない |

- 本文（24px未満、または18.66px未満のbold）: **4.5:1 以上**
- 大きいテキスト（24px以上、または18.66px以上のbold）: **3:1 以上**
- 操作可能要素の境界・状態表示・意味のある図版: **3:1 以上**
- **トークンを変更したら、この表を必ず再計算して更新する。**

### 2.4 アクセント赤の使用ルール

**[確定]（`docs/design-spec.md` から継承）**

- **`--accent` を小サイズ本文に使わない。** Off White 背景上では 4.3:1 でAA未達になる。小サイズで赤が必要なときは `--accent-dark`。
- `--accent` の許容用途: プライマリボタンの背面、24px以上の見出し内キーワード、極小面積の記号・下線・インジケーター。
- 白文字 on `--accent` はボタンと大見出しのみ可。

---

## 3. Typography

### 3.1 書体

**[確定]（継承）** 外部読み込みは **Google Fonts のみ**。追加のWebフォントサービス・アイコンフォントを増やさない。

```css
--font-sans: "Noto Sans JP", system-ui, sans-serif;
--font-mono: "IBM Plex Mono", ui-monospace, "SF Mono", Menlo, monospace;
```

- **Noto Sans JP** — 日本語本文・見出し。Weight 400 / 500 / 700 / 900。`display=swap`。
- **IBM Plex Mono** — 英数字ラベル・連番・数値の単位・キャプション。Weight 400 / 500。
- **[確定]** 日本語本文に等幅（mono）を使わない。mono は**英数字のラベル・連番・数値**に限定する。
- **[暫定]** 見出し専用書体を別途採用するか（例: 欧文見出しに別のグロテスク体を当てる）は Phase 3 で検討する。v1 では Noto Sans JP 700/900 で統一する。

### 3.2 日本語の可読性基準

**[確定]** SmartHR から借りる最重要要素。Phase 3 の3案すべてがこれを満たすこと。

| 項目 | 基準 |
| --- | --- |
| 本文サイズ | **16px 以上**（PC / SP とも）。SPで15px以下に落とさない |
| 本文 line-height | **1.8 以上**（推奨 1.9） |
| 本文 letter-spacing | **0.02em 〜 0.04em** |
| 1行の文字数 | **全角 32〜42字**（およそ 520〜680px）。これを超える幅で本文を流さない |
| 最小フォントサイズ | **12px**（注釈・monoラベルのみ）。11px以下を使わない |
| 長文の揃え | **左揃え。長文のセンター揃えは禁止**（`CLAUDE.md` §5） |
| 行の揃え | `text-align: justify` を使わない（日本語で不自然な字間が生じる） |
| 禁則・改行 | 意味の切れ目で折り返す。見出しは `<br>` ではなく `word-break` と幅制御を優先し、必要な場合のみ意図的な `<br>` を置く |

**[確定]** センター揃えを許すのは、**2行以内の短いコピー・セクションラベル・数値ブロック**のみ。

### 3.3 タイプスケール

**[暫定]** 具体的なサイズは Phase 3 で確定する。ただし**「大きな見出し × 小さな英数字ラベル」の大胆なサイズ差**を作ること自体は [確定]。

流動的サイズ（`clamp()`）を用いる。ブレークポイントごとの固定px表は持たない。

| トークン | 用途 | 値（min 〜 max） | weight | line-height | letter-spacing |
| --- | --- | --- | --- | --- | --- |
| `--fs-display` | ヒーローの主見出し | `clamp(2.5rem, 6vw, 5rem)` = 40〜80px | 900 | 1.15 | 0.01em |
| `--fs-num` | 実績数値（Novasell的表現） | `clamp(3.5rem, 9vw, 8rem)` = 56〜128px | 700 | 1.0 | -0.02em |
| `--fs-h1` | ページ見出し | `clamp(2rem, 4.4vw, 3.5rem)` = 32〜56px | 700 | 1.2 | 0.01em |
| `--fs-h2` | セクション見出し | `clamp(1.625rem, 3vw, 2.5rem)` = 26〜40px | 700 | 1.3 | 0.02em |
| `--fs-h3` | 小見出し | `clamp(1.125rem, 1.6vw, 1.375rem)` = 18〜22px | 700 | 1.5 | 0.02em |
| `--fs-lead` | リード文 | `clamp(1rem, 1.4vw, 1.125rem)` = 16〜18px | 400 | 1.9 | 0.03em |
| `--fs-body` | 本文 | `1rem` = 16px | 400 | 1.9 | 0.03em |
| `--fs-small` | 補助テキスト | `0.875rem` = 14px | 400 | 1.8 | 0.03em |
| `--fs-caption` | 注釈・出典 | `0.8125rem` = 13px | 400 | 1.7 | 0.03em |
| `--fs-label` | monoラベル・連番 | `0.75rem` = 12px | 500 | 1.0 | **0.12em** |

**[確定] 英数字ラベルの扱い**（Takram / LayerX / CADDi に共通する手法）

- セクションには **`IBM Plex Mono` の小さな英字ラベル**を添える。大文字、`letter-spacing: 0.12em`。
- 連番を持たせる（`01` / `02` …）。CADDi 的な `01 — SERVICE` 形式を基本とする。
- **[暫定]** ラベルの色は `--ink-muted`。`--accent` を使うかは Phase 3 で決める（現行 design-spec.md はアクセント色を使っていた）。
- ラベルは**見出しの上**に置き、見出しとの距離を本文行間より詰める（ひとまとまりに見せる）。

---

## 4. Spacing

### 4.1 原則

**[確定]**

- 余白は**単一のスケールから取る**。スケール外の中途半端な値（例: 37px）を使わない。
- **要素間の距離差で階層を示す。** 関連の強い要素は近く、弱い要素は遠く置く。距離が同じなら関係も同じに見える。
- **大胆な余白を取る。** 情報を詰めて埋めるより、空けて読ませる（Takram）。
- 余白を削って1画面に収めようとしない。スクロールは前提。

### 4.2 スケール

**[暫定]** 8px を基準単位とする。

```css
--space-1:  4px;
--space-2:  8px;
--space-3:  12px;
--space-4:  16px;
--space-5:  24px;
--space-6:  32px;
--space-7:  48px;
--space-8:  64px;
--space-9:  96px;
--space-10: 128px;
--space-11: 160px;
```

**[暫定] 適用の目安**

| 用途 | 値 |
| --- | --- |
| セクション上下padding | `clamp(80px, 10vw, 160px)`（= `--space-9` 〜 `--space-11` 相当） |
| セクション見出し → 本文 | `--space-6`（32px） |
| monoラベル → 見出し | `--space-3`（12px） |
| 本文の段落間 | `--space-5`（24px） |
| リスト項目間（罫線区切り） | `--space-6`（32px） |
| ボタン内padding | `16px 32px` |

---

## 5. Layout & Grid

### 5.1 原則

**[確定]**

- **全セクションをカード化しない**（`CLAUDE.md` §5）。カードは「他に構造化の手段が無い場合」の最終手段であり、既定のレイアウト単位ではない。
- 構造は **罫線・左端の整列・余白** で示す（§1.3）。
- **左端を揃える。** 見出し・ラベル・本文の左端が同じ縦線に乗ることを基本とする。
- 全幅（`100vw`）のブロックを許容する。コンテナの中にすべてを閉じ込めない。
- **1画面に情報を詰め込まない。** 1セクション1メッセージを基本とする。

### 5.2 コンテナとグリッド

**[暫定]** カラム数・コンテナ幅は Phase 3 で確定する。

```css
--container:      1240px;  /* [暫定] 標準コンテナ */
--container-text: 680px;   /* [暫定] 本文カラム最大幅（全角42字相当。§3.2） */
--container-wide: 1440px;  /* [暫定] 図版・写真の広い面 */
--gutter-pc:      64px;    /* [暫定] コンテナ左右padding（PC） */
--gutter-tb:      40px;    /* [暫定] 同（タブレット） */
--gutter-sp:      20px;    /* [暫定] 同（SP） */
--grid-cols:      12;      /* [暫定] グリッドカラム数 */
--grid-gap:       24px;    /* [暫定] カラム間 */
```

**[暫定] 想定する使い分け**

- 12カラム。本文は 6〜7カラム幅（§3.2 の文字数基準に収まること）。
- サービス・プロセスの一覧は **3カラムまたは4カラム**（LayerX観察: 4カラムグリッド）。
- ケーススタディの導入は 2カラム（テキスト : 写真 = 1 : 1 前後）。

**[確定]** カラム数を何にしても、**本文の1行文字数（§3.2）を超える幅にテキストを流してはならない。**

---

## 6. Components

**[確定] 共通原則**

- コンポーネントは**最小限の種類**に留める。同じ役割に複数の見た目を作らない。
- **角丸は控えめ。** 過剰な角丸・Pill型UIの乱用を禁止（`CLAUDE.md` §5）。
- **影を原則使わない。** 立体感ではなく、罫線と余白で分離を示す。
- **意味のないアイコンを置かない。** アイコンは意味を持つ場合のみ、かつテキストと併用する。

```css
--radius:    2px;   /* [暫定] 既定の角丸。0 も選択肢 */
--radius-lg: 4px;   /* [暫定] 写真・大きな面 */
/* pill（999px）は使わない。§10 非継承 */
```

### 6.1 Section Header

**[確定]** 全セクション共通の見出しパターン。

```
[mono英字ラベル + 連番]   例: 01 — SERVICE
[H2 日本語見出し]
（必要なら リード文 1〜2行、左揃え、--container-text 幅）
```

- ラベルは `--fs-label`、大文字、`letter-spacing: 0.12em`。
- **[暫定]** セクション上端に `--line-strong` の水平罫線を1本引くかは Phase 3 で決める（A: Editorial では有効、B: Technology ではノイズになりうる）。

### 6.2 Button / CTA

**[確定]**

| 種別 | 用途 | 仕様 |
| --- | --- | --- |
| Primary | 主CTA（「AI活用について相談する」等）。**1画面に1つ** | 背面 `--accent`、文字 `--ink-invert` |
| Secondary | 副CTA（「導入事例を見る」等） | 背景透過、`1px solid --line-strong`、文字 `--ink` |
| Text link | インライン誘導 | 下線付き。矢印記号は任意 |

- **[確定] 最小タップターゲット 44×44px**（`min-height: 44px`、推奨 48px）。
- **[確定]** CTAの文言は抽象的な「お問い合わせ」ではなく具体的な行為で書く（計画書 §11-09）。文言は `docs/CONTENT.md` の管轄であり、ここでは形だけを定義する。
- **[暫定]** 角丸 `--radius`（2px）。hover は `background` / `border-color` の変化のみ。**現行の translateY + 影拡大は非継承**（§10）。

### 6.3 Stat / Number Block

**[確定]** 実績数値の表現。Novasell / CADDi から借りる唯一の「巨大表現」。

```
[mono英字ラベル]        例: RESULT
[巨大数値 --fs-num]     例: No.1
[日本語の説明 1〜2行]   例: 国内SUBARU販売会社 商談支援AI 活用定着率
[出典・条件の注釈 --fs-caption]
```

- **[確定]** ここに置ける数値は **`docs/CONTENT.md` に記載され、掲載許諾が取れているものだけ**。デザイン上の見栄えのために数値を作らない・丸めない・単位を変えない。**架空の数字は最も重大な違反**（`CLAUDE.md` §5）。
- **[確定]** 数値だけを裸で置かない。必ず**条件（対象範囲・時期）**を注釈として添える。
- **[暫定]** 数値の色。`--ink` を基本とし、`--accent` を使うかは Phase 3 で決める（アクセント5%制約の主要な使いどころ候補）。

### 6.4 List / Index

**[確定]** サービス一覧・プロセス一覧・ケーススタディ一覧は、**カードではなくリストで組む**（Takram観察: 枠線なしで整然と列挙）。

```
─────────────────────────────
01   現場を知る
     [説明文 1〜2行]
─────────────────────────────
02   一緒につくる
     ...
```

- 区切りは `1px solid --line` の水平罫線。**囲み枠・影・背景色を持たせない。**
- 連番は mono。

### 6.5 Definition Table（会社概要等）

**[確定]（継承）** `<dl>` / `<dt>` / `<dd>` で組み、`--line` の水平罫線で行を区切る。囲み罫線を持たせない。

- **[暫定]** SP では縦組みに切り替える（`≤480px`）。

### 6.6 Mission / Statement Block

**[確定]**

- Mission / Vision は**短く大きく**（LayerX から借りる要素）。`--fs-h1` 以上。
- **2行以内ならセンター揃え可**、それを超える説明文は左揃え（§3.2）。
- **[暫定]** 反転面（`--bg-invert` に `--ink-invert`）で見せるか、白背景に大きな文字だけで見せるかは Phase 3 で決める。反転面を使う場合、Black/Navy 15〜25% の比率内に収めること。

### 6.7 Header / Footer

- **[確定]** ロゴは `assets/img/logo-horizontal.png`。ヘッダー内で不用意に拡大縮小しない。
- **[確定]** グローバルナビゲーションのラベル・階層は `docs/SITE.md`（Phase 2）の管轄。ここでは形のみ定義する。
- **[確定]** `≤768px` でハンバーガーメニュー。`aria-expanded` を持たせ、キーボードで開閉・脱出できること。
- **[暫定]** ヘッダーを sticky にするか static にするかは Phase 3 で決める。sticky にする場合は背景を不透明にし、`backdrop-filter` による過剰なガラス表現を使わない（Glassmorphism 禁止）。
- **[暫定]** ヘッダー高さ 72px（PC）/ 64px（SP）。下端に `1px solid --line`。

### 6.8 Focus / Interactive States

**[確定]** §9 Accessibility を参照。全コンポーネントに hover / focus-visible / active の状態を定義すること。**`outline: none` を単独で書かない。**

---

## 7. Photography

### 7.1 ポリシー

**[確定]** これは本サイト最大の差別化要因（「現場にいること」の視覚化）に直結するため、方向選択に関わらず不変。

| ルール | 内容 |
| --- | --- |
| **実写真を最優先** | 店舗訪問／研修／商談／現場ヒアリング／会議／業務改善／地方企業／チーム／群馬 |
| **AI生成画像を使わない** | 例外なし |
| **汎用ストックフォトを使わない** | 架空のビジネスミーティング画像等は禁止 |
| **写真が不足する面は、タイポグラフィと余白で構成する** | 大きな見出し・英数字ラベル・罫線・余白のリズムでセクションを成立させる。どうしても面が寂しい場合は単色面・図版（プロセス図・数字）で対応する |
| **実在他社のロゴを掲載しない** | 顧客企業は文字情報のみ。掲載可否は許諾の範囲内 |
| **写真枠はマークアップに明示する** | 後から差し替えやすいよう `<!-- PHOTO: 店舗訪問 -->` 形式のコメントを残す |
| **素材台帳を更新する** | 追加・差し替え時に `docs/asset-ledger.md` へ出典・加工内容・用途を記録する |

**[確定]** 写真が1枚も無い状態でも成立するデザインを作る。これはPhase 0.5（素材収集）の遅れに対する保険であると同時に、Takram的なEditorial表現とも整合する。

### 7.2 実装要件

**[確定]**

- `alt` 必須。装飾目的なら `alt=""` を明示する。
- `width` / `height` 属性を必ず指定（CLS防止）。
- ファーストビュー外の画像は `loading="lazy"`。
- 人物が写る写真は、被写体・訪問先の許諾が取れたもののみ。

### 7.3 表現

- **[確定]** 過剰なフィルター・オーバーレイ・グラデーションマスクをかけない。現場の実像を歪めない。
- **[暫定]** トリミング比率: ヒーロー横位置 `16:9`、ケーススタディ `3:2`、人物 `4:5`。
- **[暫定]** 写真の角丸: `0`（`--radius-lg` 4px も選択肢）。Phase 3 で確定。
- **[暫定]** カラーグレーディング: 無加工を基本とし、彩度・コントラストの統一のみ行う。

---

## 8. Motion

### 8.1 原則

**[確定]**

- **無意味なアニメーション・過剰なスクロール演出を禁止**（`CLAUDE.md` §5）。
- **パララックス、スクロールジャック、要素の飛び込み演出、自動再生カルーセルを使わない。**
- モーションの役割は「状態の変化を理解させる」ことのみ。装飾ではない。
- **`prefers-reduced-motion: reduce` で、すべてのトランジション・アニメーション・スムーススクロールを無効化する。**（継承）
- **外部JSアニメーションライブラリを追加しない。** Vanilla JS のみ（継承 / `CLAUDE.md` §5）。

### 8.2 許容するモーション

**[暫定]** 値は Phase 3 で調整可。

| 対象 | 仕様 |
| --- | --- |
| スクロール時のフェードイン | `IntersectionObserver` で `opacity 0→1` + `translateY 12px→0`、`0.5s`、1要素につき1回のみ（継承） |
| ボタン・リンクの hover | `background-color` / `border-color` / `color` の変化のみ、`0.2s` |
| アンカースクロール | `scroll-behavior: smooth` + `scroll-margin-top: 96px`（継承。reduced motion 時は `auto`） |
| イージング | `cubic-bezier(0.2, 0, 0, 1)` |
| duration上限 | **0.6s**。これを超える演出を作らない |

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

---

## 9. Responsive

### 9.1 ブレークポイント

**[確定]** 4点。これ以外のブレークポイントを増やさない。

| 名称 | 幅 |
| --- | --- |
| `sm` | 480px |
| `md` | 768px |
| `lg` | 1024px |
| `xl` | 1440px |

**[確定] 検証幅**（`CLAUDE.md` §4.1）: **375px / 390px / 768px / 1280px / 1440px** でスクリーンショットを取得し目視確認する。

**[暫定]** クエリの記述方式は **min-width（モバイルファースト）** を基本とする。現行 `index.html` は max-width 基準のため、Phase 4 の実装時に統一する。

### 9.2 各幅の挙動

**[確定]**

- **横スクロールを発生させない。** 全幅で `overflow-x` が生じないこと。
- **タップターゲットは 44×44px 以上。**
- SPで本文サイズを15px以下に落とさない（§3.2）。
- 見出しの流動サイズは `clamp()` の下限で頭打ちにし、SPで折り返しが破綻しないこと。

**[暫定] レイアウトの変化**

| 幅 | 変化 |
| --- | --- |
| `≥1024px` | 標準グリッド。コンテナ 1240px、gutter 64px |
| `768〜1023px` | gutter 40px。3〜4カラムの一覧は2カラムへ |
| `≤768px` | ハンバーガーメニュー（`aria-expanded`）。全カラム構成を1カラムへ。2カラムの本文＋写真は縦積み（写真を下） |
| `≤480px` | gutter 20px。CTAは幅100%の縦積み。定義テーブルは縦組み |

---

## 10. Accessibility

**[確定]** 全項目が方向選択に依存しない基準。`CLAUDE.md` §4.3 の **Lighthouse Accessibility 95以上**と整合させる。

### 10.1 基準

| 項目 | 基準 |
| --- | --- |
| 準拠レベル | **WCAG 2.1 レベル AA** |
| 本文コントラスト | **4.5:1 以上**（24px未満、または18.66px未満のbold） |
| 大きいテキスト | **3:1 以上**（24px以上、または18.66px以上のbold） |
| UIコンポーネント・意味のある図版の境界 | **3:1 以上** |
| タップターゲット | **44×44px 以上** |
| Lighthouse Accessibility | **95以上** |

### 10.2 フォーカス表示

**[確定]**

- すべての操作可能要素に `:focus-visible` のスタイルを定義する。
- **`outline: none` を単独で書かない。** 消す場合は必ず代替の可視表示を同時に定義する。
- 既定のフォーカスリング: `outline: 2px solid var(--focus); outline-offset: 2px;`
- 反転面（`--bg-invert`）上では `--focus` を `--ink-invert` に切り替え、3:1 以上を確保する。
- キーボードのみで、すべてのCTA・ナビゲーション・メニュー開閉を操作でき、フォーカスが視覚的に追えること。フォーカストラップを作らない。

### 10.3 構造

**[確定]**

- `<html lang="ja">`。
- **H1はページに1つ。** 見出しレベルを飛ばさない（h2の次にh4を置かない）。見た目のサイズで見出しレベルを選ばない。
- ランドマーク（`header` / `nav` / `main` / `footer`）を使う。`main` はページに1つ。
- **本文へのスキップリンク**を先頭に置く（SmartHR観察）。フォーカス時のみ可視でよい。
- リンクテキストだけで行き先が分かること。「こちら」単独のリンクを作らない。
- 別タブで開くリンクには、その旨を伝える（`aria-label` または視覚的な注記）。
- 装飾のみの要素は支援技術から隠す（`aria-hidden="true"`）。

### 10.4 モーション・その他

**[確定]**

- `prefers-reduced-motion: reduce` に対応する（§8.1）。
- 自動再生する動画・カルーセルを置かない。
- **色だけで情報を伝えない**（§2.1）。
- ズーム 200% で情報・機能が失われないこと。
- フォームは持たない（問い合わせはGoogleフォーム）。将来サイト内フォームを持つ場合は `<label>` の関連付け・エラーのテキスト明示を必須とし、`/security-review` を実施する。

---

## 11. Do & Don't

### 11.1 Do

**[確定]**

- 余白・タイポグラフィのサイズ差・罫線で階層を作る
- 左端を揃える
- セクションに mono英字ラベル + 連番を添える
- 日本語本文は 16px / line-height 1.8以上 / 全角32〜42字で組む
- 実績数値は `docs/CONTENT.md` の事実のみ、条件の注釈付きで大きく見せる
- 実際の現場写真を使う。無ければタイポグラフィと余白で成立させる
- 1セクション1メッセージ
- CTAは具体的な行為で書く
- トークンに無い値が必要になったら、実装より先に本ファイルへ追記する（人間の確認を経て）

### 11.2 Don't

**[確定]**（`CLAUDE.md` §5 / 計画書 §9 と同一。**このリストは本ファイル側で増やさない。増やす場合は `CLAUDE.md` §5 を更新し、ここは参照に留める**）

**ビジュアル**

- 紫・青のAIグラデーション
- 光る球体
- AI脳・ロボット・回路図の汎用イメージ
- 架空のビジネスミーティング画像・汎用ストックフォト・AI生成画像
- 過剰なGlassmorphism / 過剰な角丸 / Pill型UIの乱用
- **全セクションをカード化する**
- 意味のないアイコン
- **長文の中央揃え**
- 無意味なアニメーション・過剰なスクロール演出

**コンテンツ（特に厳守）**

- 架空の導入実績 / 架空の数字 / 架空の顧客コメント / 架空の受賞歴
- 実在他社ロゴの掲載

**その他**

- アクセント赤（`--accent`）を小サイズ本文に使う
- 外部JSライブラリ・フレームワークの追加（Vanilla JSのみ。外部読み込みはGoogle Fontsのみ）

---

## 12. `docs/design-spec.md` からの継承 / 非継承

現行サイトのデザイン仕様のうち、何を引き継ぎ、何を捨てるかの判断。**`docs/design-spec.md` 自体は現行実装の記録として残す**（§0.2）。

### 12.1 継承するもの

| 項目 | 継承の理由 | 扱い |
| --- | --- | --- |
| **Noto Sans JP + IBM Plex Mono の2書体構成** | 日本語の可読性（SmartHR）と英数字ラベル（Takram / CADDi）の両方を満たす。外部依存を増やさずに済む | [確定]（見出し専用書体の追加検討のみ [暫定]） |
| **CSS変数（`:root`）によるトークン管理** | エージェントが値を1箇所で変更できる。AIネイティブ運用の前提 | [確定] |
| **アクセント赤を小サイズ本文に使わない / 必要なら `--accent-dark`** | コントラスト不足。Off White 背景上で 4.3:1 と実測でAA未達 | [確定]（§2.4） |
| **ロゴ赤 `#DE2B1C` をアクセントの起点にする** | ブランドの実測値。アクセントを1色に限定する原則とも整合 | 起点の考え方は [確定]、具体値は [暫定] |
| **外部JSライブラリ・フレームワーク禁止（Vanilla JSのみ、外部読み込みはGoogle Fontsのみ）** | パフォーマンスと保守性。GitHub Pages + 素のHTML構成を維持する方針と整合 | [確定] |
| **`prefers-reduced-motion` 対応** | アクセシビリティ必須要件 | [確定]（§8.1） |
| **`IntersectionObserver` による軽いフェードイン（translateY 12px→0 / 0.5s）** | 外部ライブラリ不要で、演出として過剰でない | [暫定]（§8.2） |
| **`scroll-behavior: smooth` + `scroll-margin-top`** | アンカー遷移時の見出し隠れを防ぐ実務的な措置 | [暫定]（値のみ） |
| **横スクロール禁止 / タップターゲット44px以上** | アクセシビリティ・品質チェックの基本 | [確定]（§9.2 / §10.1） |
| **数値・固有名詞の創作禁止 / 実在他社ロゴの掲載禁止** | 事実の取り扱いはリニューアル後も同一 | [確定]（§11.2） |
| **`<dl>`/`<dt>`/`<dd>` + 罫線による会社概要テーブル** | セマンティクスとして正しく、罫線主体の構成とも整合 | [確定]（§6.5） |

### 12.2 非継承にするもの

| 項目 | 非継承の理由 |
| --- | --- |
| **カード様式（`--card` 背景 + 1.5px ink枠 + オフセット影）を基本レイアウト単位とする構成** | 計画書 §2.1 が「カード型UIへの依存」を現状の問題として名指ししている。§9 でも「全セクションをカード化する」を禁止。Takramから借りる要素の筆頭が「カードUIへの非依存」。**リニューアルの目的そのものと衝突する** |
| **ベントグリッド（bento / modular）レイアウト** | カード依存の一形態。「理由1が2行スパン、理由3がアクセント反転」といった構成は、余白と構図ではなくブロックの配置で階層を作る手法であり、Editorial表現と両立しない |
| **アクセント反転カード（赤ベタ塗り面）** | Brand Accent 5%以下の面積制約（§2.1）に反する。現行は「理由3」「フロー中央」等で赤の大面積ベタ塗りを使っている |
| **オフセット影 `--shadow: 4px 4px 0`** | ワイヤーフレーム案C「手描き／インクボーダー」表現の名残。Precise / Premium と矛盾する。新デザインでは影を原則使わない（§6） |
| **角丸 `--radius: 10px` / `--radius-pill: 999px`** | 「過剰な角丸」「Pill型UIの乱用」を禁止（§11.2）。参考4社に共通して角丸は極小または不使用（LayerX観察: 角丸ほぼ不使用）。`--radius: 2px` へ |
| **方眼グリッド背景（32px格子の `repeating-linear-gradient`）** | 「テック・グリッド」という旧コンセプト固有の装飾。新コンセプト **Field × Technology** では、テクノロジーは「Understated（控えめ）」に表現する。装飾で先進性を主張しない |
| **ベージュ寄りのパレット（`--paper #F6F4EE` / `--beige #EFE9E2` / `--card #FCFBF7`）** | White/Off White 70〜80% の原則自体は同じだが、暖色ベージュ寄りの色相は Editorial / Precise / Premium よりクラフト・温かみ寄りに見える。よりニュートラルな Off White（`#F7F6F3`）へ。※色相の最終判断は Phase 3（特にDirection Cでは再検討の余地あり） |
| **ブレークポイントごとの固定pxタイプスケール表** | 3段階の固定値では中間幅で破綻する。`clamp()` による流動スケールへ（§3.3）。※方式の変更であり、値そのものは [暫定] |
| **ボタンhoverの `translateY(-2px)` + 影拡大 / カードhoverの影拡大** | 影を使わない方針と矛盾。また移動を伴うhoverは「無意味なアニメーション」に近い。色の変化のみへ（§8.2） |
| **チップ（`ISSUE 01` 等のpill型ラベル）** | Pill型UIの乱用に該当。連番は mono英字ラベルとして枠なしで扱う（§3.3） |
| **セクション別レイアウト仕様表（01 Hero 〜 Footer の具体的構成）** | これは**デザインではなく情報設計**であり、`docs/SITE.md`（Phase 2）の管轄。かつ1ページLPからコーポレートサイト型へ移行するため（計画書 §10）、現行のセクション定義自体を引き継がない |
| **コンテナ 1140px / gutter 56px** | 値としては近いが、より大胆な余白と広い図版面を取るため 1240px / 64px を初期値とする。※ [暫定] |

---

## 13. Phase 3 で確定させる項目（チェックリスト）

Phase 3 のデザイン探索と Phase 4 のトップページ実装を経て、以下の [暫定] を確定し、本ファイルを v2 へ更新する。

- [ ] `--ink` をニュートラルブラックとネイビーのどちらにするか（§2.2）
- [ ] アクセントカラーの具体値と、使用箇所の確定（§2.2 / §2.4 / §6.3）
- [ ] Off White の色相（ニュートラル寄り / 暖色寄り）（§2.2 / §12.2）
- [ ] 見出し専用書体を追加するか（§3.1）
- [ ] タイプスケールの具体値（§3.3）
- [ ] monoラベルの色（`--ink-muted` / `--accent`）（§3.3）
- [ ] グリッドカラム数とコンテナ幅（§5.2）
- [ ] セクション上端の水平罫線の有無（§6.1）
- [ ] ヘッダーの sticky / static、高さ（§6.7）
- [ ] Mission ブロックを反転面にするか（§6.6）
- [ ] 実績数値の色（§6.3）
- [ ] 写真のトリミング比率と角丸（§7.3）
- [ ] モーションのduration / easing の最終値（§8.2）
- [ ] メディアクエリの記述方式（min-width への統一時期）（§9.1）

確定のたびに、該当箇所のマーカーを **[確定]** に書き換え、§2.3 のコントラスト表を再計算する。
