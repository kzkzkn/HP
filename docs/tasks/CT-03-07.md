# Codex Task

## 実行規律（厳守）
- 説明・計画だけのメッセージを出力するとセッションが終了する。**メッセージ出力は最後の完了報告1回のみ**とし、それまではツール呼び出し（ファイル編集・コマンド実行）を連続して行うこと。
- ローカルサーバーは **http://localhost:8099 で起動済み**。http-serverの起動・停止は行わないこと。
- スクリーンショットは `npx playwright screenshot ...` をそのまま実行（ブラウザはインストール済み）。

Task ID: CT-03〜CT-07
対象ページ: index.html
対象セクション: 01ヒーロー（CT-03）／02課題（CT-04）／03解決策・道のり（CT-05）／04サービス（CT-06）／05選ばれる理由ベントグリッド（CT-07）

目的: メインコンテンツ前半5セクションを design-spec.md の数値仕様どおりに実装する。

参照デザイン: docs/design-spec.md「3. レイアウト・セクション仕様」の該当行（必読）／docs/content.md（**文言は一字一句転記。創作・要約禁止**）

使用素材: なし（ビジュアルはすべてインラインSVGで実装。docs/asset-ledger.md のB分類を参照）

対象ファイル: index.html（既存セクションの中身を実装）、assets/css/style.css（追記）、assets/js/main.js（IntersectionObserverフェードイン追加）

実装要件:
1. 【CT-03 #hero】方眼背景（repeating-linear-gradient 32px格子、線色--grid）。2カラム（左1.1:右1、gap30px）。左: mono「AI SOLUTION PARTNER」→小見出し→H1（content.md記載の改行位置、「使いこなせる会社へ」をaccent色のspanで）→サブ文→CTA2本（.btn「お問い合わせ」／.btn-ghost「資料請求」、どちらもGoogleフォームへtarget=_blank）。右: カード（--card、inkボーダー、影）内に mono風コメント装飾なしで、抽象ダッシュボードのインラインSVG（折れ線グラフ・棒グラフ・チェックリスト風。色は--ink/--line/--accentのみ、文字なし、aria-hidden="true"、role="presentation"）。上下padding PC110px/90px。
2. 【CT-04 #issues】H2＋ISSUEカード6枚（PC3col×2row、gap18px、カードpadding22px）。各カード: 上段にチップ「ISSUE 01」〜「ISSUE 06」（mono）と36pxのSVGアイコン（線画stroke1.5px --ink、モチーフ: 吹き出し/分岐矢印/人物+?/時計/本/虫眼鏡）、H3なしで本文（content.mdの6項目）をそのまま。
3. 【CT-05 #solution】背景--beige（セクション全幅）。(a)フロー: 3ボックス「課題」「AIの鉄人」「成果」を横並び中央、中央ボックスはaccent塗り白文字、ボックス間はSVG矢印。(b)「6ヶ月で『自分たちで使いこなせる』までの道のり」見出し＋STEP1〜3の縦タイムラインカード（左にaccentバッジ「STEP 1」+期間mono表示、右に見出しと箇条書き。content.mdの全箇条書きを省略せず転記）。(c)注記文。(d)「6ヶ月後、貴社はこうなっています」+チェック4項目（2col、SVGチェックマークaccent）。
4. 【CT-06 #service】前置き文（AIの鉄人とは）＋3カード（3col）: SVGアイコン56px＋H3＋本文。content.mdの範囲内の文言のみ。
5. 【CT-07 #reasons】ベントグリッド: PC3col・grid-auto-rows:132px・gap18px。配置: 理由1=grid-row:span 2、理由2=1マス、理由3=1マス（accent塗り・白文字）、理由4=grid-column:span 2、理由5・6=1マスずつ。各カード: H3（番号付き見出し）＋本文（content.md転記）。
6. フェードイン: IntersectionObserverで .reveal クラスの要素を translateY(12px)→0 / opacity 0→1（0.5s）。prefers-reduced-motion:reduce では即時表示。main.jsに実装。
7. レスポンシブ: design-spec「4. レスポンシブ」どおり（≤768: ヒーロー1col・課題/サービス2col・ベント2col・フロー縦積み＋矢印回転、≤480: 全1col・CTA幅100%）。

変更してはいけない箇所: ヘッダー・フッターの既存実装、docs/、assets/img/、:rootトークン定義（追加は可、変更は不可）

PCでの達成条件（1440px）: 5セクションがspec表の構成・数値で表示。ベントのスパン配置が指定どおり。横スクロールなし。
スマートフォンでの達成条件（390px）: 全カードが1col縦積み、フローは縦並び、文字溢れ・横スクロールなし。

アクセシビリティ要件: 見出し階層h1→h2→h3を守る。装飾SVGはaria-hidden。チェックリストはul/li。accent塗りカード内の文字は白でコントラスト確保。

検証コマンド（サーバーは起動済み、起動・停止不要）:
- npx playwright screenshot --viewport-size=1440,900 --full-page http://localhost:8099 shots/ct0307-1440.png
- npx playwright screenshot --viewport-size=768,1024 --full-page http://localhost:8099 shots/ct0307-768.png
- npx playwright screenshot --viewport-size=390,844 --full-page http://localhost:8099 shots/ct0307-390.png

スクリーンショット取得条件: 上記3枚（full-page）

完了条件: 5セクション実装済み、スクショ3枚取得、コンソールエラーなし。完了報告に変更ファイル／検証結果／スクショ場所／デザインとの既知差異を含める。
