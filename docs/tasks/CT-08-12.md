# Codex Task

## 実行規律（厳守）
- 説明・計画だけのメッセージを出力するとセッションが終了する。**メッセージ出力は最後の完了報告1回のみ**。それまでツール呼び出しを連続実行すること。
- ローカルサーバーは http://localhost:8099 で起動済み。http-serverの起動・停止は行わない。
- スクリーンショット取得は不要（レビュアー側で実施する）。curl -s http://localhost:8099 でHTML確認は可。

Task ID: CT-08〜CT-12
対象ページ: index.html
対象セクション: 06実績（CT-08）／07ミッション（CT-09）／08会社・代表（CT-10）／09問い合わせ＋フッター（CT-11）／SEO・アクセシビリティ・最終調整（CT-12）

目的: 後半セクションを完成させ、SEO/a11y/OGPを整備してページ全体を完成状態にする。

参照デザイン: docs/design-spec.md（該当行）／docs/content.md（**文言転記、創作禁止**）

使用素材:
- assets/img/ceo-photo.jpg（08 代表メッセージ、幅360pxカラム、object-fit:cover、角丸--radius、alt="代表取締役 後藤築"）
- assets/img/contact-qr.png（09、表示140px、alt="お問い合わせフォームのQRコード"）
- assets/img/logo-horizontal.png（フッター、高さ28px）
- assets/img/logo-circle.png（favicon・OGP用）

対象ファイル: index.html、assets/css/style.css、assets/js/main.js（必要時のみ）

実装要件:
1. 【CT-08 #case】背景--beige。(a)ハイライト大カード: mono「CASE」→H2→「商談支援AIツールの活用定着率」＋「国内SUBARU販売会社 1位」（「1位」をnum-xl 56px accent。これ以外の数値表現は作らない）＋「を達成（富士スバル様）」。(b)事実カード2枚（content.md記載の2文）。(c)事例表カード: dl表組で「業種」「成果」（content.mdの表内容）。
2. 【CT-09 #mission】宣言カード1枚（padding48px、中央寄せ）: mono「MISSION」→宣言文30px/700（SP22px）。
3. 【CT-10 #company】(a)代表メッセージ: 2カラム（写真360px＋本文）。mono「CEO MESSAGE」→本文→署名。(b)会社概要: dl/dt/ddテーブル（社名/所在地/代表者/事業内容/MAIL/HP、罫線--line。MAILはmailtoリンク、HPはテキストのみ）。
4. 【CT-11 #contact＋footer】contact: カード1枚に H2→誘導文→CTA2本（.btn「お問い合わせフォームへ」／.btn-ghost「資料請求（サービス資料をお送りします）」、Googleフォームへtarget=_blank rel=noopener）→QR画像140px＋「お問い合わせはこちらから」＋mailtoリンク。フォーム入力UIは実装しない。footer: 背景#EFECE4、上ボーダー1.5px ink、ロゴ＋ナビ再掲＋「© 2026 株式会社AIの鉄人」。
5. 【CT-12 SEO/a11y】
   - head: title・meta description（content.mdメタ情報）、OGP（og:title/og:description/og:type=website/og:url=https://www.ainotetsujin.com//og:image=assets/img/logo-circle.png絶対URL）、twitter:card=summary、canonical、theme-color=#F6F4EE
   - JSON-LD（Organization: name/url/logo/address/email、チラシ記載の範囲のみ）
   - favicon: logo-circle.pngをlink rel="icon"に（追加生成不要）
   - 全imgにwidth/height属性（CLS防止）、loading="lazy"（ヒーロー外）、alt精査
   - skip link（「本文へスキップ」、視覚的に隠しフォーカス時表示）
   - 横スクロール最終確認（1440/1024/768/390）
6. ページ全体の見出し階層・セクション間余白の最終整合。

変更してはいけない箇所: 既存セクション01〜05の構造・文言、docs/、assets/img/、:rootトークン値

PCでの達成条件（1440px）: 4セクション＋フッターがspecどおり表示。ページ全体を通してセクション背景（paper/beige交互）が正しい。
スマートフォンでの達成条件（390px）: 代表メッセージは写真上・本文下の1col、会社概要dlは縦組、CTA縦積み100%、横スクロールなし。

アクセシビリティ要件: dl構造の正しさ、リンクの識別性、QR画像のalt、フッターnavにaria-label、コントラスト（beige上の--soft文字は13px以上）。

検証コマンド:
- curl -s http://localhost:8099 で全セクション・メタタグの出力確認
- JSON-LDが妥当なJSONであること（python3 -c などで検証）

スクリーンショット取得条件: 不要（レビュアーが取得）

完了条件: 全セクション完成、スクショ4枚、コンソールエラーなし、リンク・画像切れなし。完了報告に変更ファイル／検証結果／スクショ場所／既知差異／未解決事項。
